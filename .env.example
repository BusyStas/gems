# Flask Configuration
SECRET_KEY=your-secret-key-here
FLASK_DEBUG=False

# Site Configuration
SITE_URL=https://preciousstone.info

# Google Analytics (Optional)
GOOGLE_ANALYTICS_ID=

# Google Search Console Verification (Optional)
GOOGLE_SEARCH_CONSOLE_VERIFICATION=
# GEMDB API key (for local dev only). In Cloud Run, store the secret in GCP Secret Manager.
# This can be a single key or a mapping in the form: gems_hub:KEY,desktop_app:KEY2
# For local development convenience, if this environment variable isn't set and you keep a
# gemhunter/dev config at '../gemhunter/config.json' with 'gemdb_api_token', the app will
# pick the gems_hub key from that file automatically.
# Example (do not commit real keys):
# GEMDB_API_KEY=gems_hub:YOUR_KEY,desktop_app:OTHER_KEY
GEMDB_API_KEY=
GCP_PROJECT_ID=
